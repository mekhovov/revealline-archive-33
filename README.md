# RevealLine Archive33: v0.64.3 and proposed v0.65.0 append

Archive33 v0.64.3 is accepted at `b184d8d8872ec305d8606ac99bd3d6235e291db8`, tree `b46cb2fa286731828733a65f0b83f6b205f8e3f8`: 712 public files / 313,572,608 bytes, complete public-byte verification and scoped native acceptance. The original `input-authority.json` remains unchanged as historical preparation evidence. The separate `append-v0650-authority.json` records this additive proposal and pins both accepted predecessor and main-game records; future append commit, deployment and acceptance remain pending.

The accepted v0.65.0 imported-Team continuation cohort uses stable release 392203520, source `57fdc39aa78683f6de479eb77f810f60e7c6756a`, tree `b71a23d6c2474da72ef379793c811e7097b0a156` and annotated tag `74721a379e648f796e58a01fadf193f2467c2375`. Fresh read-only metadata confirms all nine original asset IDs, sizes and digests; four original metadata/qualification files remain byte-exact. Main acceptance covers only the recorded imported-Team win, picture, Next and Retry journey.

Expected combined inventory: **1,421 files / 627,143,902 bytes**, leaving 172,856,098 bytes within the unchanged 800,000,000-byte limit. All 712 previous routes remain: 709 original v0.64.3 runtime/metadata/qualification rows / 313,571,214 bytes plus two support files are byte-identical. Only the root index changes, by 79 bytes, to list both versions. The new v0.65.0 cohort adds 709 rows / 313,571,215 bytes. Three combined support rows total 1,473 bytes.

The workflow fetches both exact locked release tags, v0.64.3 and v0.65.0. Existing production prepare/verify helpers, the independent inventory fixture file, pinned extractor tooling `a13ab970222498d7c5fa7f62f9fc04fe436979d5`, historical metadata and release-explorer bridge are unchanged. The 3 GiB hosted capacity floor, 20,000-row/800 MB limits, CRC/hash and hidden-file checks, original ZIP transport and non-cancelling concurrency are preserved. The committed metadata fixture retains original cohort assertions and adds the exact appended identity and totals, following the accepted Archive32 append. No game rebuild, local distribution download or source TAR extraction is part of this append.

Root must review the finite patch, qualify and merge the isolated infrastructure PR, retain both original extraction receipts, and verify every public file with stable before/after authorities. Observe the appended Team entry and retained Workshop entry through ordinary native navigation before accepting the successor archive. Only then may a separately reviewed main publisher update this archive admission and assign v0.65.0 canonical ownership. Existing main catalog routes remain unchanged here. Physical input, live rescue/difficulty, responsiveness, audible listening, offline use and parent-phase completion remain separate gates.
